{% extends 'base.html.twig' %}

{% block body %}
    <h1>Config edit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Save</button>
    {{ form_end(form) }}
{% endblock %}