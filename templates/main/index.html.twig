{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-grid gap-3">
        {% for monitor in monitors %}
            <a
                role="button"
                href="{{ path('app.main.toggle_monitor', {"monitorId": monitor.mid}) }}"
                class="btn {{ isMonitorActive(monitor.mid) ? 'btn-success' : 'btn-outline-danger' }}"
            >{{ monitor.name }}</a>
        {% endfor %}
    </div>
{% endblock %}