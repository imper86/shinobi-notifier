{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit {{ senderService.name }} #{{ key }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-outline-success">Save</button>
    {{ form_end(form) }}
{% endblock %}

